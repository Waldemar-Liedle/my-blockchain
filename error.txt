 FAIL  wallet\index.test.js
  ● Wallet › calculating balance › calculates the balance for blockchain transactions matching the recipient

    expect(received).toEqual(expected)

    Expected value to equal:
      NaN
    Received:
      500

      80 |         it('calculates the balance for blockchain transactions matching the recipient', () => {
      81 |             expect(wallet.calculateBalance(bc))
    > 82 |                 .toEqual(INITIAL_BALANCE + (addBalance * repeatAdd));
      83 |         });
      84 |
      85 |         it('calculates the balance for blockchain transactions matching the sender', () => {

      at Object.it (wallet/index.test.js:82:18)

  ● Wallet › calculating balance › calculates the balance for blockchain transactions matching the sender

    expect(received).toEqual(expected)

    Expected value to equal:
      NaN
    Received:
      500

      85 |         it('calculates the balance for blockchain transactions matching the sender', () => {
      86 |             expect(senderWallet.calculateBalance(bc))
    > 87 |                 .toEqual(INITIAL_BALANCE - (addBalance * repeatAdd));
      88 |         });
      89 |
      90 |     });

      at Object.it (wallet/index.test.js:87:18)